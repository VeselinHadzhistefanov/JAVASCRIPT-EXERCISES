# Exercises Folder - Function Reference

## Files and Functions

### countBs.js
| Function | Description | Testable |
|----------|-------------|----------|
| `countBs1(input)` | Counts 'B' characters in a string using for-in loop | No (not exported) |
| `countBs2(input)` | Counts 'B' characters in a string using for-of loop | No (not exported) |

### isEven.js
| Function | Description | Testable |
|----------|-------------|----------|
| `isEvenVar(num)` | Checks if a number is even using recursion (function expression) | No (not exported) |
| `isEven(num)` | Checks if a number is even using recursion (function declaration) | No (not exported) |
| `getIterations(num)` | Returns the number of iterations (num / 2) | No (not exported) |

### Minimum.js
| Function | Description | Testable |
|----------|-------------|----------|
| `getMinimum(...args)` | Logs array elements passed as arguments | No (not exported) |

### printFarmAnimals.js
| Function | Description | Testable |
|----------|-------------|----------|
| `printFarmAnimals(cows, chickens, pigs)` | Prints formatted count of farm animals | Yes (exported) |
| `padNumber(num, size)` | Pads a number with leading zeros to a specified size | No (not exported) |

### printCalendar.js
| Function | Description | Testable |
|----------|-------------|----------|
| `print(arg)` | Writes argument to stdout | No (not exported) |
| `getDaySubpiece(dayOfMonth)` | Formats day number with padding | No (not exported) |
| `printYearDay(dayOfYear)` | Prints a single day in calendar format | No (not exported) |
| `printCalendar()` | Prints a full year calendar to stdout | No (not exported) |
|----------|-------------|----------|
### forIn.js
| Function | Description | Testable |
|----------|-------------|----------|
| N/A | Demonstrates for...of with entries() and traditional for loop | No (demonstration only) |

### objectEnumTest.js
| Function | Description | Testable |
|----------|-------------|----------|
| N/A | Demonstrates for...in loop over arrays with custom properties | No (demonstration only) |

### testObj.js
| Function | Description | Testable |
|----------|-------------|----------|
| N/A | Demonstrates object destructuring | No (demonstration only) |
